# Add a constructor for JSON deserialization to all Lombok classes
lombok.anyConstructor.addConstructorProperties=true

# Let static analysis tools know which classes are generated by Lombok
lombok.addLombokGeneratedAnnotation=true